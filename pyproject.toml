[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-prompt"
version = "0.1.0"
description = "Tile context to LLM prompt assembly with budget management"
license = {text = "MIT"}
requires-python = ">=3.8"
